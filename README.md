# Codex Skills

個人管理の global Codex skills を、責務ごとのカテゴリに分けて保管する repository です。

```text
skills/
├── governance/  # 指示・規約・検証ゲート
└── management/  # skill の発見・改善・運用管理
```

各 skill は `SKILL.md` を起点とし、必要な `agents/`、`references/`、`scripts/`、`assets/` を同じディレクトリ内に保持します。
